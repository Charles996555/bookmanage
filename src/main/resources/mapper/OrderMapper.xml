<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jincheng.bookmanage.dao.OrderDao">
	<insert id="saveAddMessage">
		insert into `order` values (0,#{ordernumber},#{username},#{createtime},#{updatetime},#{totalprice},#{ispay})
	</insert>
</mapper>